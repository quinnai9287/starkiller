export default function (canvasContainer: any) {
  const drawWidth = () => {};
  const drawCBLine = (backLength: number) => {};
  const drawCF = () => {};
  const drawBL = () => {};

  retrun {
    drawWidth,
    drawCBLine,
    drawCF,
    drawBL
  }
}
